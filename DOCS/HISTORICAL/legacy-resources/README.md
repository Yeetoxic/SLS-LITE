# Historical Runtime Resources

These files came from the pre-modernization SLS-LITE resource directory. They
are retained only as migration and implementation references.

Current SLS-LITE does not load these files, and Maven does not package them in
the plugin JAR. Active defaults live in `src/main/resources`.
